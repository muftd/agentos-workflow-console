# Workflow Console · v0.2 需求规划

> **状态**: 🔄 规划中
>
> **规划日期**: 2025-11-18
>
> **目标**: 从"单一演示"到"实用工具"的跨越

## 1. 需求背景

### v0.1 现状总结
- ✅ 单个 session 的可视化展示
- ✅ 基础交互（点击、键盘导航）
- ✅ Figma/Framer 级别的 UI 质量
- ❌ 只读模式，无法编辑
- ❌ 只支持一个 workflow
- ❌ 数据完全静态（hard-coded JSON）

### v0.2 的核心转变
```
v0.1: 演示工具（Demo Tool）
      ↓
v0.2: 实用工具（Practical Tool）

从"展示一个高光案例"
到"管理和编辑多个日常任务流"
```

## 2. 用户提出的核心需求

### 需求1: 多 Session 支持 ⭐⭐⭐

**问题陈述**:
> 当前产品只支持一个 session（一条 workflow）。我希望这个产品可以刻画出，我日常工作流中的**每个任务**的闭环流转链条 - 这个任务起始点是哪个 coach、中间经过哪些节点，最终在哪个 coach 结束完成。我每天会有很多任务。

**功能需求**:
- 可以切换查看不同任务的链条
- 按照创建日期进行排序，最新的在前
- 交互形式：抽屉 menu 或其他方案

**我的分析**:
- ✅ **优先级**: 非常高，这是从演示到实用的关键
- ✅ **价值**: 符合愿景剧本中"一天结束前保存为 Workflow v0.3"的场景
- ✅ **可行性**: 技术实现相对直接，schema 已支持多 session
- ⚠️ **依赖**: 需要数据持久化方案支持

### 需求2: 前台编辑操作 ⭐⭐⭐

**问题陈述**:
> 当前 coach 之间没打通（上下文隔离），无法自动沉淀数据，只能我手动维护 flow 和 node 数据。

**功能需求**:
- 对 flow: 新建/删除/编辑（session 级别）
- 对 node: 新建/删除/编辑（step 级别）

**我的分析**:
- ✅ **优先级**: 非常高，是实用化的必要条件
- ✅ **价值**: 解决了当前"只能通过修改 JSON 文件更新数据"的痛点
- ⚠️ **复杂度**: 涉及表单设计、数据验证、状态管理
- ⚠️ **依赖**: 强依赖数据持久化方案

## 3. 我对 v0.2 的补充需求

基于愿景剧本和实际使用场景，我认为 v0.2 还应包括：

### 需求3: 数据持久化方案 ⭐⭐⭐

**问题陈述**:
编辑功能的前提是有地方保存数据。当前是静态 JSON 文件，无法支持运行时编辑。

**技术选项对比**:

| 方案 | 优点 | 缺点 | 建议 |
|------|------|------|------|
| **LocalStorage** | 简单，纯前端，无需后端 | 容量限制 5MB，无法跨设备 | ✅ v0.2 首选 |
| **IndexedDB** | 容量大，性能好 | API 复杂，需要封装 | 🔄 v0.3 考虑 |
| **后端 API** | 功能完整，可跨设备同步 | 需要服务器，成本高 | ❌ v0.2 不考虑 |

**推荐方案**: LocalStorage + JSON export/import

**理由**:
- v0.2 聚焦核心功能，保持纯前端部署
- LocalStorage 足够应对单用户、单设备场景
- 通过 export/import 提供数据备份和迁移能力
- 为未来引入后端预留接口设计

### 需求4: Session 元数据增强 ⭐⭐

**功能需求**:
- 为 session 添加标签（tags）
- 添加状态标识（进行中/已完成/已归档）
- 添加优先级标识（高/中/低）
- 支持按标签筛选和搜索

**价值**:
- 当 session 数量增多时（10+），方便管理和查找
- 符合愿景剧本中"工作流谱库"的概念

### 需求5: 导入/导出功能 ⭐⭐

**功能需求**:
- 导出当前所有 sessions 为 JSON 文件
- 从 JSON 文件导入 sessions
- 导出单个 session
- 批量导入

**价值**:
- 数据备份和迁移
- 跨设备使用（通过文件传输）
- 与其他工具集成的可能性

## 4. v0.2 功能清单（优先级排序）

### P0 (Must Have) - 核心功能

#### 1. 多 Session 管理基础 ⭐⭐⭐
- [ ] Session 列表 UI（侧边栏/抽屉）
- [ ] Session 切换功能
- [ ] 按创建日期排序
- [ ] 显示 session 元数据（标题、日期、步骤数）
- [ ] 当前 session 高亮显示

#### 2. 数据持久化（LocalStorage）⭐⭐⭐
- [ ] 设计 LocalStorage 数据结构
- [ ] 实现 CRUD 接口封装
- [ ] 自动保存机制
- [ ] 数据迁移逻辑（从静态 JSON 到 LocalStorage）

#### 3. Session 编辑功能 ⭐⭐⭐
- [ ] 新建 session（表单：title, description）
- [ ] 编辑 session 元数据
- [ ] 删除 session（带确认对话框）
- [ ] 复制 session

#### 4. Step 编辑功能 ⭐⭐⭐
- [ ] 新建 step（表单：actor, tool, skill, input_label, output_label, summary）
- [ ] 编辑现有 step
- [ ] 删除 step
- [ ] Step 排序调整（拖拽或按钮）

### P1 (Should Have) - 增强功能

#### 5. Session 元数据增强 ⭐⭐
- [ ] 添加 tags 字段
- [ ] 添加 status 字段（in-progress/completed/archived）
- [ ] 添加 priority 字段（high/medium/low）
- [ ] 标签筛选 UI
- [ ] 状态筛选 UI

#### 6. 导入/导出功能 ⭐⭐
- [ ] 导出所有 sessions 为 JSON
- [ ] 导入 JSON 文件
- [ ] 导出单个 session
- [ ] 数据校验和错误处理

#### 7. 搜索和过滤 ⭐
- [ ] 按标题搜索 sessions
- [ ] 按 actor 过滤 steps
- [ ] 按日期范围过滤

### P2 (Nice to Have) - 锦上添花

#### 8. 用户体验优化 ⭐
- [ ] 编辑模式切换（查看模式 vs 编辑模式）
- [ ] 撤销/重做功能
- [ ] 快捷键支持（Ctrl+N 新建，Ctrl+S 保存）
- [ ] 空状态提示（首次使用引导）

#### 9. 数据可视化增强 ⭐
- [ ] Session 统计（总数、完成率、平均步骤数）
- [ ] Actor 使用频率统计
- [ ] 时间线视图（按日期显示 sessions）

## 5. UI/UX 设计方案

### 5.1 Session 切换 UI 方案对比

#### 方案A: 侧边栏抽屉（推荐）⭐⭐⭐

```
┌────────────────────────────────────┐
│  [☰ Sessions]  Workflow Console    │  ← Header with menu trigger
├────────────────────────────────────┤
│                                    │
│  ┌──────────┐                      │
│  │ Session  │  Flow Map            │
│  │ List     │  (StepNodes...)      │
│  │          │                      │
│  │ • Task 1 │                      │
│  │ • Task 2 │                      │
│  │ • Task 3 │                      │
│  │          │                      │
│  │ [+ New]  │                      │
│  └──────────┘                      │
│                                    │
│  Step Detail Panel                 │
└────────────────────────────────────┘
```

**优点**:
- 不占用主界面空间
- 符合现代 Web App 习惯
- 可以展示丰富信息
- 支持搜索和过滤

**缺点**:
- 需要点击展开

#### 方案B: 顶部下拉选择器

```
┌────────────────────────────────────┐
│  [Task 1 ▼]  Workflow Console      │  ← Dropdown selector
├────────────────────────────────────┤
│  Flow Map (StepNodes...)           │
│                                    │
│  Step Detail Panel                 │
└────────────────────────────────────┘
```

**优点**:
- 简洁直观
- 快速切换

**缺点**:
- 难以展示元数据（日期、tags 等）
- 不支持搜索和过滤
- Session 数量多时体验差

#### 方案C: 独立 Session 列表页

```
Page 1: Session List               Page 2: Session Detail
┌──────────────────────┐          ┌────────────────────────┐
│ All Workflows        │  Click   │ Task 1                 │
│ ┌──────────────────┐ │  ──────► │ Flow Map               │
│ │ Task 1           │ │          │ Step Detail            │
│ │ 2025-11-18       │ │          └────────────────────────┘
│ │ 6 steps          │ │
│ └──────────────────┘ │
│ ┌──────────────────┐ │
│ │ Task 2           │ │
│ └──────────────────┘ │
└──────────────────────┘
```

**优点**:
- 清晰的信息架构
- 易于浏览和管理

**缺点**:
- 增加页面层级
- 切换需要跳转

**推荐方案**: **方案A（侧边栏抽屉）**

**理由**:
1. 保持单页应用体验
2. 可展示丰富的 session 元数据
3. 支持搜索、过滤、排序
4. 符合 Figma/Framer 等工具的交互习惯

### 5.2 编辑模式设计

#### 模式切换

```
┌────────────────────────────────────┐
│  Sessions  |  Workflow Console     │
│  [View Mode] [Edit Mode] ← Toggle  │
├────────────────────────────────────┤
│                                    │
│  Edit Mode:                        │
│  • Each node has [Edit] [Delete]   │
│  • Flow Map has [+ Add Step]       │
│  • Auto-save indicator             │
│                                    │
└────────────────────────────────────┘
```

#### Step 编辑表单（Modal/Dialog）

```
┌─────────────────────────────────────┐
│  Edit Step #01                   [X]│
├─────────────────────────────────────┤
│  Actor:     [Coach C         ▼]    │
│  Tool:      [Claude Code     ▼]    │
│  Skill:     [__________________]    │
│  Input:     [__________________]    │
│             [__________________]    │
│  Output:    [__________________]    │
│             [__________________]    │
│  Summary:   [__________________]    │
│             [__________________]    │
│             [__________________]    │
│  Tags:      [tag1] [tag2] [+ Add]   │
│                                     │
│          [Cancel]  [Save]           │
└─────────────────────────────────────┘
```

## 6. 数据结构设计

### 6.1 LocalStorage 结构

```typescript
// Key: 'workflow-console-v0.2'
interface AppData {
  version: '0.2';
  sessions: WorkflowSession[];
  settings: {
    currentSessionId: string | null;
    lastUpdated: string;
  };
}

interface WorkflowSession {
  session_id: string;
  title: string;
  description: string;
  created_at: string;
  updated_at: string;  // 新增

  // v0.2 新增字段
  status?: 'in-progress' | 'completed' | 'archived';
  priority?: 'high' | 'medium' | 'low';
  tags?: string[];

  steps: Step[];
}
```

### 6.2 数据迁移策略

```typescript
// 从 v0.1 的静态 JSON 迁移到 v0.2 的 LocalStorage
function migrateFromV01() {
  // 1. 检查 LocalStorage 是否有数据
  // 2. 如果没有，从 /data/workflow-log-sample.json 加载
  // 3. 添加 v0.2 新字段的默认值
  // 4. 保存到 LocalStorage
}
```

## 7. 技术实现要点

### 7.1 状态管理

```typescript
// 使用 React Context + useReducer
interface AppState {
  sessions: WorkflowSession[];
  currentSessionId: string | null;
  isEditMode: boolean;
  isSidebarOpen: boolean;
}

type Action =
  | { type: 'SET_SESSIONS'; payload: WorkflowSession[] }
  | { type: 'SELECT_SESSION'; payload: string }
  | { type: 'ADD_SESSION'; payload: WorkflowSession }
  | { type: 'UPDATE_SESSION'; payload: { id: string; data: Partial<WorkflowSession> } }
  | { type: 'DELETE_SESSION'; payload: string }
  | { type: 'TOGGLE_EDIT_MODE' }
  | { type: 'TOGGLE_SIDEBAR' };
```

### 7.2 LocalStorage 封装

```typescript
// client/src/lib/storage.ts
export class WorkflowStorage {
  private static KEY = 'workflow-console-v0.2';

  static load(): AppData | null;
  static save(data: AppData): void;
  static clear(): void;

  // Session CRUD
  static getSessions(): WorkflowSession[];
  static getSession(id: string): WorkflowSession | null;
  static addSession(session: WorkflowSession): void;
  static updateSession(id: string, updates: Partial<WorkflowSession>): void;
  static deleteSession(id: string): void;

  // Export/Import
  static exportAll(): string;  // JSON string
  static importAll(json: string): void;
}
```

### 7.3 表单验证

```typescript
// client/src/lib/validation.ts
export function validateSession(data: Partial<WorkflowSession>): ValidationResult {
  const errors: string[] = [];

  if (!data.title || data.title.trim().length === 0) {
    errors.push('Title is required');
  }

  if (data.title && data.title.length > 100) {
    errors.push('Title must be less than 100 characters');
  }

  // ... more validations

  return {
    isValid: errors.length === 0,
    errors
  };
}
```

## 8. 开发计划

### 阶段拆分

#### Stage 1: 数据层重构（2-3天）
- 设计 LocalStorage 数据结构
- 实现 WorkflowStorage 封装
- 数据迁移逻辑
- 单元测试

#### Stage 2: 多 Session UI（2-3天）
- Sidebar 组件开发
- Session 列表渲染
- Session 切换功能
- 搜索和过滤 UI

#### Stage 3: Session 编辑功能（2-3天）
- Session 表单组件
- 新建/编辑/删除逻辑
- 表单验证
- 错误处理

#### Stage 4: Step 编辑功能（2-3天）
- Step 表单组件
- 新建/编辑/删除逻辑
- Step 排序功能
- 集成测试

#### Stage 5: 导入/导出（1-2天）
- Export 功能
- Import 功能
- 文件校验
- 用户提示

#### Stage 6: 优化和测试（2-3天）
- 性能优化
- 用户体验打磨
- 全面测试
- 文档更新

**总计**: 约 11-17 天（2-3周）

## 9. 成功标准

### 功能标准
- [ ] 可以创建、编辑、删除多个 sessions
- [ ] 可以在 sessions 之间切换
- [ ] 可以编辑 steps（新建、修改、删除、排序）
- [ ] 数据可持久化，刷新页面不丢失
- [ ] 支持导入/导出 JSON

### 质量标准
- [ ] 所有操作有明确的用户反馈
- [ ] 表单有客户端验证和错误提示
- [ ] 删除操作有确认对话框
- [ ] UI 质量保持 Figma/Framer 级别
- [ ] 响应式设计在移动端正常工作

### 性能标准
- [ ] LocalStorage 读写流畅（< 100ms）
- [ ] Session 切换无延迟（< 200ms）
- [ ] 支持至少 50 个 sessions

## 10. 风险和待确认事项

### 需要用户确认的问题：

#### Q1: 数据持久化方案
**问题**: 使用 LocalStorage 意味着数据只存在浏览器本地，无法跨设备访问，清除浏览器数据会丢失。

**选项**:
- A. LocalStorage + 手动导出/导入（v0.2 推荐）
- B. 引入简单后端（如 Firebase/Supabase）
- C. 其他方案？

**我的建议**: 选项 A，v0.2 保持纯前端，通过导出/导入解决数据迁移问题。

#### Q2: 编辑功能范围
**问题**: 编辑功能应该多丰富？

**最小版本**:
- 基础表单（文本输入）
- 简单验证
- 保存/取消

**完整版本**:
- 实时预览
- 撤销/重做
- 自动保存
- 版本历史

**我的建议**: v0.2 先做最小版本，保证核心功能可用。

#### Q3: Session 列表 UI
**问题**: 使用侧边栏抽屉还是其他方案？

**我的建议**: 侧边栏抽屉（Sheet 组件），理由见 Section 5.1。

#### Q4: 是否需要撤销/重做？
**问题**: 实现成本较高，是否 v0.2 必须？

**我的建议**: v0.2 不做，放到 v0.3。用"编辑前预览"和"确认对话框"降低误操作风险。

## 11. 与愿景剧本的对应关系

### 愿景剧本核心要素

| 愿景要素 | v0.2 对应功能 | 状态 |
|---------|--------------|------|
| 多条工作流 | 多 Session 管理 | ✅ P0 |
| 保存编排 | Session 新建/编辑 | ✅ P0 |
| 查看产出 | Step 详情展示 | ✅ v0.1 已有 |
| 查看 Prompt/skill | Step skill 字段 | ✅ v0.1 已有 |
| 复用编排 | Session 复制功能 | ✅ P0 |
| 工作流谱库 | Session 列表 + 标签 | ✅ P0 + P1 |
| 时间轴 | 按日期排序 + 过滤 | ✅ P0 + P1 |
| 音乐工作站式轨道 | ❌ v0.3+ 考虑 | ❌ 超出 v0.2 |
| 自动编排 | ❌ 远期愿景 | ❌ 需 AI 能力 |

**结论**: v0.2 实现了愿景的**核心管理和编辑能力**，为未来的高级功能（轨道视图、自动编排）打下基础。

## 12. 下一步行动

### 需要用户确认：

1. ✅ 同意 v0.2 的整体方向和优先级划分？
2. ✅ 数据持久化使用 LocalStorage 方案？
3. ✅ Session 切换 UI 使用侧边栏抽屉？
4. ✅ 编辑功能做最小版本（无撤销/重做）？

### 确认后的工作：

1. 创建 `docs/v0.2-plan.md`（详细开发计划）
2. 更新 schema（添加 v0.2 新字段）
3. 开始 Stage 1 开发（数据层重构）

---

**期待你的反馈！** 🚀
